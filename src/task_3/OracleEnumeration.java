package task_3;

public class OracleEnumeration {
    /**
     * Oracle
     * Перелічувані типи (enums): https://docs.oracle.com/javase/tutorial/java/javaOO/enum.html
     * Перелічування (enum) – це перелік іменованих констант.
     * Java-перелічування можуть мати конструктори, методи та змінні екземпляра.
     *  public enum Day {
     * SUNDAY, MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY
     * }
     * Можна оголосити змінну перелічуваного типу (enum), якій можна буде надати ТІЛЬКИ значення,
     * які оголошені в цьому типі.
     * Щоб використовувати екземпляр перелічуваного типу, необхідно створити змінну цього типу
     * та присвоїти їй одне з значень:
     * Day dayToday = Day.MONDAY;
     * System.out.print(“Today is ” + dayToday);
     */
}
